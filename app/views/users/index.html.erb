<%= simple_form_for :search, url: @users_path, method: "GET" do |f| %>
  <%= f.input :name, input_html: {value: ""} %>
  <%= f.submit "Search" %>
<% end %>

<ul>
  <% @users.each do |user| %>
    <li>
      <% if user.profile_pic.present? %>
        <%= image_tag user.profile_pic_url(:thumb) %>
      <% end %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>

<%# / h1 User List
/ h4 Experts (have linked Stripe Accounts)
/ ul
/   - @experts.each do |user|
/     li
/       = link_to user.name, user
/ h4 All Other Users
ul
  - @users.each do |user|
    li
      = link_to user.name, user %>
